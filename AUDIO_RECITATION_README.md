# نظام التلاوة الصوتية - Quran App

## نظرة عامة

تم إضافة نظام تلاوة صوتية شامل لتطبيق القرآن الكريم يتضمن:
- ✅ تشغيل التلاوة لأكثر من 8 قراء مشهورين
- ✅ مزامنة الصوت مع النص وتظليل الآية الحالية
- ✅ تحميل الصوت للاستخدام دون اتصال
- ✅ حفظ تقدم المستخدم واستئناف الاستماع
- ✅ مشغل صوتي كامل مع التحكم في السرعة والتكرار
- ✅ مشغل مصغر عائم يظهر في جميع الصفحات

---

## البنية التقنية

### المكتبات المستخدمة
- `expo-av`: لتشغيل الملفات الصوتية
- `expo-file-system`: لتحميل وإدارة الملفات الصوتية
- `@react-native-async-storage/async-storage`: لحفظ التفضيلات والتقدم

**ملاحظة:** تم استبدال `@react-native-community/slider` بشريط تقدم مخصص باستخدام View components لتجنب مشاكل التوافق.

### الهيكل الملفي

```
app/
├── player.tsx              # صفحة المشغل الكامل
├── reciters.tsx           # صفحة اختيار القراء وإدارة التحميلات
└── quran.tsx              # محدثة: أزرار التشغيل + تظليل الآية

components/
└── MiniPlayer.tsx         # المشغل المصغر العائم

contexts/
└── AudioPlayerContext.tsx # Context لإدارة حالة المشغل عبر التطبيق

services/
├── AudioService.ts        # إدارة تشغيل الصوت والـ streaming
├── StorageService.ts      # إدارة التخزين المحلي والتحميلات
└── TimingService.ts       # إدارة توقيتات المزامنة

assets/
├── reciters.json          # بيانات القراء المتاحين
└── timings/               # ملفات توقيت المزامنة
    └── 001.json           # مثال: توقيتات سورة الفاتحة
```

---

## القراء المتاحون

1. **عبد الباسط عبد الصمد** (192kbps - مرتل)
2. **ماهر المعيقلي** (64kbps - مرتل)
3. **محمد صديق المنشاوي** (128kbps - مرتل)
4. **مشاري راشد العفاسي** (64kbps - مرتل)
5. **محمود خليل الحصري** (128kbps - مرتل)
6. **محمد أيوب** (64kbps - مرتل)
7. **عبد الرحمن السديس** (192kbps - مرتل)
8. **أبو بكر الشاطري** (128kbps - مرتل)

---

## مصدر الصوت

يستخدم التطبيق **Everyayah.com API** للحصول على التلاوات:
```
https://everyayah.com/data/[reciter]/[surah][verse].mp3
```

مثال:
```
https://everyayah.com/data/Abdul_Basit_Murattal_192kbps/001001.mp3
```

---

## الميزات الرئيسية

### 1. التشغيل الأساسي
- تشغيل آية محددة من صفحة القرآن
- التبديل بين تشغيل/إيقاف مؤقت
- الانتقال للآية التالية/السابقة
- التحكم في موقع التشغيل (seek)

### 2. المزامنة والتظليل
- تظليل تلقائي للآية أثناء التلاوة
- تمييز بصري واضح (خلفية خضراء + حد ذهبي)
- تحديث فوري عند تغيير الآية

### 3. سرعة التشغيل
- 0.5x (نصف السرعة)
- 0.75x
- 1.0x (السرعة العادية)
- 1.25x
- 1.5x
- 1.75x
- 2.0x (ضعف السرعة)

### 4. أوضاع التكرار
- **لا تكرار**: توقف عند نهاية الآية
- **تكرار الآية**: إعادة نفس الآية
- **تكرار السورة**: إعادة السورة كاملة
- **تكرار الكل**: الاستمرار في القرآن كاملاً

### 5. التحميل للاستخدام دون اتصال
- تحميل سور كاملة لأي قارئ
- عرض حجم التحميل والمساحة المستخدمة
- إدارة التحميلات (حذف/إضافة)
- التبديل التلقائي بين streaming والملفات المحلية

### 6. حفظ التقدم
- حفظ آخر موضع استماع تلقائياً
- عرض "استئناف الاستماع" في الصفحة الرئيسية
- حفظ تفضيلات المستخدم (القارئ، السرعة، التكرار)

---

## واجهات المستخدم

### المشغل المصغر (Mini Player)
- يظهر في أسفل الشاشة عبر جميع الصفحات
- يعرض: اسم السورة، رقم الآية، اسم القارئ
- أزرار: السابق، تشغيل/إيقاف، التالي
- شريط تقدم صغير
- الضغط عليه يفتح المشغل الكامل

### المشغل الكامل (Full Player)
- بانر جميل للسورة مع أيقونة
- عرض نص الآية الحالية
- معلومات القارئ (قابلة للتغيير)
- شريط تقدم تفاعلي مع الوقت
- أزرار التحكم الكاملة
- اختيار السرعة والتكرار

### صفحة القرآن المحدثة
- زر تشغيل/إيقاف بجانب كل آية
- تظليل تلقائي للآية المشغلة
- زر قائمة لخيارات إضافية (تشغيل، حفظ، نسخ)

### صفحة القراء
- قائمة بجميع القراء مع معلوماتهم
- عرض حالة التحميل لكل قارئ
- أزرار تحميل/حذف
- تحديد القارئ الافتراضي
- عرض الحجم المستخدم

---

## كيفية الاستخدام

### للمستخدم العادي

1. **بدء الاستماع:**
   - اذهب لصفحة القرآن
   - اضغط على زر ▶️ بجانب أي آية
   - سيظهر المشغل المصغر في الأسفل

2. **تغيير القارئ:**
   - اضغط على المشغل المصغر لفتح المشغل الكامل
   - اضغط على اسم القارئ الحالي
   - اختر القارئ الذي تريده

3. **التحميل للاستخدام دون اتصال:**
   - اذهب لقائمة القراء (من الصفحة الرئيسية)
   - اختر القارئ واضغط "تحميل"
   - انتظر حتى ينتهي التحميل

4. **استئناف الاستماع:**
   - في الصفحة الرئيسية، اضغط على "استئناف الاستماع"
   - سيعود للموضع الأخير تلقائياً

---

## للمطورين

### إضافة قارئ جديد

أضف القارئ في `assets/reciters.json`:
```json
{
  "id": "new_reciter",
  "name": "الاسم بالعربية",
  "name_en": "Name in English",
  "apiPath": "Reciter_Path_On_Everyayah",
  "quality": "128kbps",
  "style": "مرتل",
  "subfolder": "Reciter_Path_On_Everyayah"
}
```

### إضافة ملف توقيت لسورة

أنشئ ملف `assets/timings/XXX.json` (XXX = رقم السورة بـ 3 خانات):
```json
{
  "surah": 1,
  "name": "الفاتحة",
  "verses": [
    {
      "id": 1,
      "startTime": 0,
      "endTime": 4500,
      "duration": 4500
    }
  ]
}
```

### استخدام AudioPlayerContext

```typescript
import { useAudioPlayer } from '../contexts/AudioPlayerContext';

function MyComponent() {
  const { state, playVerse, togglePlayPause } = useAudioPlayer();
  
  // تشغيل آية
  await playVerse(surahId, verseId, autoPlay);
  
  // تشغيل/إيقاف
  await togglePlayPause();
  
  // الحالة الحالية
  console.log(state.isPlaying);
  console.log(state.currentSurahId);
  console.log(state.currentVerseId);
}
```

---

## التحديات والحلول

### 1. حجم الملفات الكبير
**الحل:**
- استخدام streaming افتراضياً
- تحميل انتقائي (10 سور فقط)
- عرض تحذير قبل التحميل

### 2. مزامنة دقيقة
**الحل:**
- استخدام ملفات timing JSON
- إمكانية إضافة ملفات توقيت من مصادر موثوقة
- نظام افتراضي عند غياب الملف

### 3. التشغيل المتواصل
**الحل:**
- استخدام Context API عالمي
- مثيل واحد من AudioService
- MiniPlayer يظهر في جميع الصفحات

### 4. التخزين المحلي
**الحل:**
- هيكل واضح للملفات في FileSystem
- تتبع دقيق للمساحة المستخدمة
- إمكانية الحذف السهل

---

## خارطة الطريق المستقبلية

### قريباً
- [ ] إضافة إشعارات التحكم في الخلفية
- [ ] دعم التشغيل في الخلفية الكامل
- [ ] إضافة ملفات توقيت لجميع السور
- [ ] تحسين animations الانتقال

### متوسط الأجل
- [ ] دعم قراءات مختلفة (حفص، ورش، إلخ)
- [ ] إضافة التفسير الصوتي
- [ ] إمكانية إنشاء قوائم تشغيل
- [ ] مشاركة المقاطع الصوتية

### طويل الأجل
- [ ] دعم الترجمات الصوتية
- [ ] تحميل ذكي (AI-based)
- [ ] مزامنة بين الأجهزة
- [ ] وضع الحفظ مع التكرار الذكي

---

## الاختبار

### اختبار التشغيل الأساسي
```bash
# تأكد من تثبيت المكتبات
npm install

# شغل التطبيق
npm start
```

### نقاط الاختبار الرئيسية
- ✅ تشغيل آية واحدة
- ✅ التبديل بين الآيات
- ✅ تغيير القارئ أثناء التشغيل
- ✅ التحميل والاستماع دون اتصال
- ✅ حفظ واستعادة الموضع
- ✅ التظليل المتزامن
- ✅ التحكم في السرعة
- ✅ أوضاع التكرار

---

## الدعم والمساهمة

لأي استفسارات أو مشاكل:
1. تحقق من console للأخطاء
2. تأكد من الاتصال بالإنترنت للـ streaming
3. تحقق من المساحة المتاحة للتحميل

---

## الاعتمادات

- **Everyayah.com**: مصدر التلاوات الصوتية
- **Tanzil.net**: مرجع لبيانات القرآن
- **Expo & React Native**: البنية التحتية للتطبيق

---

**تم التطوير بـ ❤️ لخدمة كتاب الله**

